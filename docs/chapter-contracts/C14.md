# C14 contract — Critical on Average, Broken by Direction

## Opening phenomenon

Gaussian and orthogonal deep-linear products have comparable average scale but
radically different singular edges.

## Diagnostic chain

Derive the wide-model second-moment recursion; identify the ReLU critical
scale; reinterpret it as trace control; compare with the complete Jacobian
spectrum and dynamical isometry.

## Evidence and theorems

- `c14-criticality-001`, class (a), seeded CPU matrix control.
- `c14-gated-product-001`, class (a), hash-seeded finite-width ReLU gate
  product that exposes the rank loss hidden by the deep-linear control.
- `thm-c14-variance-recursion`, diagnostic proof sketch.
- `thm-c14-average-spectrum`, full proof.
- Harness `ch-14`, API 0.14.

## Boundaries

The gated witness is a finite diagnostic, not a distributional theorem. No
finite nonlinear network is claimed to follow the scalar recursion exactly.
No generic FP16-depth failure or random-matrix law for trained Jacobian
products is imported from slides.

## Handoff

C15 asks what scale-control maps erase and how their Jacobians alter the path.
