# C15 contract — Normalization Chooses an Invariance

## Opening phenomenon

Centered and RMS-only coordinate normalization respond differently to a
constant shift; raw-moment FP32 variance simultaneously repeats C02's
cancellation failure.

## Diagnostic chain

Name axes and statistics; derive invariances and zero-epsilon Jacobians; expose
rank-$d-1$ versus rank-$d-2$ behavior; add epsilon, precision, running-state,
and residual-placement contracts.

## Evidence and theorem

- `c15-invariance-001`, class (a), deterministic vector/Jacobian arithmetic.
- `thm-c15-normalization-jacobians`, full proof.
- Harness `ch-15`, API 0.15.

## Boundaries

Landscape smoothing is scoped paper evidence, not a universal theorem. An
explicit identity summand in pre-normalization is algebra, not a universal
training guarantee. Standard names appear only in the sanctioned bridge.

## Handoff

C16 follows curvature after local signal-control machinery is in place.
